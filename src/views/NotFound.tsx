import React from 'react'

interface Props {}

function NotFound(props: Props): React.ReactElement { 
    const {} = props

    return (
        <div>
            NotFound
        </div>
    )
}

export default NotFound
