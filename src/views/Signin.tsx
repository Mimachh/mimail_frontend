import React from 'react';

interface Props {}

function Signin(props: Props): React.ReactElement { // Ajoutez la mention de React.ReactElement
    const {} = props;

    return (
        <div>
            Signin
        </div>
    );
}

export default Signin;
